#  somehow using a yml file here will let the test fail - the merging does not seem to work properly
spring.datasource.url=jdbc:sqlite:file::memory:?cache=shared

logging.level.root=INFO

re-director.auth.enabled=true
re-director.auth.additional-permit-all-paths=/test/**,/additional-path/**